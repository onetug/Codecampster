@page
@model RegisterSpeakerModel
@{
    ViewData["Title"] = "Speaker Registration";
}

<h2>@ViewData["Title"]</h2>
<hr />

<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Input.Password"></label>
                <input asp-for="Input.Password" class="form-control" />
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Input.ConfirmPassword"></label>
                <input asp-for="Input.ConfirmPassword" class="form-control" />
                <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
            </div>

            @*<div class="form-group" style="width: 200px; height: 60px;">
            @{
                string imgSrc = string.Empty;
                if (!string.IsNullOrEmpty(Model.Input.CaptchaBase64Data))
                {
                    imgSrc = String.Format("data:image;base64,{0}", Model.Input.CaptchaBase64Data);
                }
            }
            <img src="@imgSrc" style="width: 200px; height: 60px;" />
        </div>

        <div class="form-group">
            <input asp-for="Input.EnteredCaptchaCode" class="form-control" value="" />
        </div>*@

            <div class="form-group">
                <button type="submit" class="btn btn-default">Register</button>
            </div>
            <environment exclude="Development">

                <div class="g-recaptcha" data-sitekey="@Model.Input.CaptchaKey"></div>
            </environment>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
